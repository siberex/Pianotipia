import {
    customElement,
    html,
    property,
    LitElement,
} from 'lit-element';

@customElement('piano-key')
export class PianoKey extends LitElement {
    render() {
        return html`<div></div>`;
    }
}
